export function Logo({ className = 'h1 mt-3 mb-4 pb-3' }) {
  return (
    <p className={`${className} text-center text-primary-emphasis`}>
      <i class="bi bi-book"></i>
      Meus Livros
    </p>
  )
}
